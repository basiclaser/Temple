var timeCheck = 600;

// Check if link is another page or just internal
 var timeChecker = function(){
   if(a is another page && (a != google || duckduckgo || wolfram alpha || bing)){
     if(timeCheck == 0){
        function(){
            // this.open a new tab
            timeCheck = 600;
        };
      };else{alert("please wait")};
    };
  };


$("a").click(timeChecker())
